# Only exclude files that are truly unnecessary or sensitive

# Environment variables (keep these private)
.env
.env.local
.env.production
.env.staging

# Python cache and compiled files (can be regenerated)
__pycache__/
*.py[cod]
*$py.class

# Virtual environment (too large and can be recreated)
venv/
env/
ENV/

# Node.js dependencies (too large, can be reinstalled)
node_modules/

# Next.js build output (can be regenerated)
.next/
out/

# IDE files (personal preferences)
.vscode/
.idea/

# OS files (system-specific)
.DS_Store
Thumbs.db

# Temporary deployment files
.vercel
